name: CI

on: [push]

jobs:
  build:

    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: Restore
      run: restore.cmd
    - name: Build
      run: build.cmd
    - name: Test
      run: test.cmd
